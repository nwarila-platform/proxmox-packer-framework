version: 2
updates:

  # GitHub Actions — workflow steps and composite actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    target-branch: "main"
